import { Routes } from '@angular/router';
import { ImageSelectionPageComponent } from './image-selection-page.component';

export const routes: Routes = [
  {
    path: '',
    component: ImageSelectionPageComponent,
  },
];
